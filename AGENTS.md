# Gambl Balatro

Read `.agent` and `Gamble_Balatro_PRD.md`. The user's Three.js/WebGL and Python stack overrides the PRD's PixiJS requirement. Prioritize menu and mobile visibility. Run `npm test` and `npm run build`; run `scripts/browser-check.mjs` against the dev server for layout and interaction verification. Keep scoring logic independent of UI. Do not claim the full PRD is complete while planned modes remain disabled. Preserve all other games when integrating the hub.
